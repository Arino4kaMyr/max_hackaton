# MAX Efficiency Bot

Чат-бот для MAX, который помогает:

- создавать и удалять задачи с дедлайном и описанием;
- заводить события, управлять напоминаниями и очищать их при необходимости;
- запускать и останавливать помодоро-таймеры, следить за активными сессиями;
- получать ежедневные сводки и управлять уведомлениями;
- просматривать полные списки задач/событий и быстро возвращаться в главное меню.

## Быстрый старт

1. Склонируйте репозиторий, перейдите в папку `bot` и установите зависимости:
   ```bash
   cd bot
   npm install
   ```

2. Создайте `.env` в корне репозитория (рядом с `README.md`) на основе `env.example` и добавьте токен из карточки бота:
   ```
   BOT_TOKEN=ваш_токен
   TIMEZONE=Europe/Moscow
   ```

3. Запустите бота (всё ещё находясь в `bot/`):
   ```bash
   npm start
   ```
   Для разработки удобно использовать автоматический перезапуск:
   ```bash
   npm run dev
   ```

## Основные сценарии

### Главная клавиатура
После команды `/start` или действия «⬅️ В меню» бот показывает только 2 кнопки:

1. **📋 Задачи и события** – переход в хаб управления списками и созданием.
2. **⏱ Помодоро таймер** – экран состояния таймера (запустить/остановить).

### Задачи
- Дедлайны вводятся в любом из форматов `25.11.2025 18:00`, `25.11.2025`, `25.11 18:00`, `2025-11-25 18:00`.
- Задачи сохраняются в памяти процесса (при перезапуске бота информация очищается — для продакшена замените хранилище на базу).

### События и уведомления
- Для каждого события можно выбрать время напоминания в минутах.
- Напоминание создаётся через `node-schedule`.
- Ежедневный дайджест отправляется в 09:00 указанного часового пояса (по умолчанию Europe/Moscow) и содержит задачи на сегодня + ближайшие события.

### Помодоро
- Помодоро запускается только для существующих задач.
- Таймер работает в несколько циклов (по умолчанию 4) и сообщает о начале каждого рабочего/перерывного интервала.
- Команда `/stop` прерывает таймер.

### Списки и управление
- В хабе «📋 Задачи и события» бот печатает весь список задач и событий и показывает кнопки «Создать задачу», «Создать событие», «Удалить задачу», «Удалить событие», а также ссылку на «⚙️ Настройки».
- После создания или удаления бот автоматически обновляет список, чтобы сразу увидеть изменения.

### Таймеры
- Экран `⏱ Помодоро таймер` всегда один: если таймер не запущен — бот предложит выбрать режим («🚀 По задаче» или «🧠 Свободный»). При активном цикле отображаются подробности и кнопка «⏹ Остановить таймер».
- Режим «🚀 По задаче» запускает таймер на выбранной задаче, а «🧠 Свободный» не требует задач — просто интервалы работы/отдыха.
- Команда `/stop` и кнопка «⏹ Остановить таймер» завершают текущий цикл и обновляют экран состояния.

### Настройки
- Включение/выключение ежедневного дайджеста.
- Быстрый выбор времени напоминания (15/30/60 минут).
- Ввод собственного часового пояса (например, `Europe/Moscow`).
- Всегда можно вернуться на главный экран через кнопку «⬅️ В меню» или команду `/menu` / `/cancel`.

## Тестирование

1. Запустите бота локально (`npm start`) и откройте чат MAX.
2. Проверьте сценарии:
   - `/start` + нажатие обеих кнопок на главном экране.
   - Создание задачи и события, проверка текстовых форматов.
   - Запуск и остановка помодоро (`/stop`).
   - Переключатели в «Настройках» + смена часового пояса.
3. Для проверки напоминаний временно задайте близкие даты (например, на несколько минут вперёд).

## Что дальше

- Подключите реальную базу данных или persistent storage.
- Добавьте авторизацию для команд (например, админские функции).
- Расширьте виды уведомлений и типов вложений (клавиатуры, ссылки на мини-приложения).

Удачи на хакатоне!