# MAX Productivity Bot

Чат-бот для MAX мессенджера с функциями управления задачами, событиями, Pomodoro таймером и уведомлениями.

## Возможности

- ✅ **Управление задачами** - создание, просмотр, удаление задач с дедлайнами
- 📅 **События** - планирование событий с напоминаниями
- 🍅 **Pomodoro таймер** - таймер продуктивности с настраиваемыми циклами
- 🔔 **Уведомления** - ежедневные дайджесты и напоминания о событиях
- 💾 **PostgreSQL** - надежное хранение данных через Prisma ORM
- 🐳 **Docker** - легкий запуск через Docker Compose

## Требования

- Docker и Docker Compose (для запуска через Docker)
- Или Node.js 18+ и PostgreSQL (для локального запуска)
- Токен бота MAX мессенджера

**Подробный список требований:** см. [requirements.txt](./requirements.txt)

## Настройка

### 1. Создание файла .env

Создайте файл `.env` в корне проекта на основе примера:

```bash
cp .env.example .env
```

Затем отредактируйте `.env` и укажите реальный токен бота:

```env
# Бот (ОБЯЗАТЕЛЬНО!)
BOT_TOKEN=your_actual_bot_token_here
```

**Важно:** 
- Замените `your_actual_bot_token_here` на реальный токен вашего бота MAX мессенджера
- Файл `.env` уже добавлен в `.gitignore` и не будет закоммичен в репозиторий
- При запуске бот автоматически проверит наличие обязательных переменных и выдаст понятную ошибку, если что-то не настроено

**Дефолтные значения (уже встроены в код):**
- База данных: `postgres` / `postgres` / `productivity_bot` (пользователь/пароль/название)
- Порт БД: `5432`
- Часовой пояс: `Europe/Moscow`
- Порт приложения: `3000`

Эти значения используются автоматически, если не указаны в `.env`. Для изменения используйте переменные:
- `DB_USER`, `DB_PASSWORD`, `DB_NAME`, `DB_PORT` - параметры БД
- `TIMEZONE` - часовой пояс
- `PORT` - порт приложения

### 2. Получение токена бота

1. Откройте MAX мессенджер
2. Найдите раздел для разработчиков ботов
3. Создайте нового бота и получите токен
4. Вставьте токен в файл `.env`

## Запуск через Docker Compose (рекомендуется)

Это самый простой способ запуска проекта. Docker Compose автоматически настроит базу данных и запустит бота.

### Команды для сборки и запуска

#### Первый запуск (сборка образа)

```bash
docker compose up --build
```

Эта команда:
- Соберет Docker образ бота
- Запустит контейнер PostgreSQL
- Применит миграции базы данных
- Запустит бота

#### Последующие запуски

```bash
docker compose up
```

#### Запуск в фоновом режиме

```bash
docker compose up -d
```

#### Остановка контейнеров

```bash
docker compose down
```

#### Остановка с удалением данных

```bash
docker compose down -v
```

**Внимание:** Команда `-v` удалит все данные из базы данных!

#### Просмотр логов

```bash
# Все логи
docker compose logs

# Логи бота
docker compose logs bot

# Логи в реальном времени
docker compose logs -f bot

# Логи базы данных
docker compose logs db
```

#### Пересборка после изменений

```bash
docker compose up --build --force-recreate
```

### Примеры запуска через командную строку

#### Пример 1: Полный запуск с пересборкой

```bash
# Остановить текущие контейнеры (если запущены)
docker compose down

# Запустить с пересборкой
docker compose up --build
```

#### Пример 2: Запуск в фоне с просмотром логов

```bash
# Запустить в фоне
docker compose up -d

# Просмотреть логи
docker compose logs -f bot
```

#### Пример 3: Перезапуск только бота

```bash
# Перезапустить только контейнер бота
docker compose restart bot

# Или остановить и запустить заново
docker compose stop bot
docker compose start bot
```

#### Пример 4: Проверка статуса контейнеров

```bash
# Показать статус всех контейнеров
docker compose ps

# Показать детальную информацию
docker compose ps -a
```

## Локальный запуск (без Docker)

Если вы предпочитаете запускать проект локально без Docker:

### Требования

- Node.js 18 или выше
- PostgreSQL 15 или выше
- npm или yarn

### Шаги для локального запуска

#### 1. Установка зависимостей

```bash
# В корне проекта
npm install
```

#### 2. Настройка базы данных

Убедитесь, что PostgreSQL запущен и доступен. Создайте базу данных:

```bash
# Подключитесь к PostgreSQL
psql -U postgres

# Создайте базу данных
CREATE DATABASE productivity_bot;

# Выйдите из psql
\q
```

#### 3. Настройка .env для локального запуска

В файле `.env` укажите `DATABASE_URL` для локальной базы данных:

```env
# Для локального запуска используйте localhost
DATABASE_URL="postgresql://postgres:postgres@localhost:5432/productivity_bot?schema=public"

BOT_TOKEN=ваш_токен_бота_здесь
TIMEZONE=Europe/Moscow
PORT=3000
```

#### 4. Применение миграций

```bash
# Сгенерировать Prisma Client
npm run prisma:generate

# Применить миграции
npm run migrate
```

#### 5. Запуск бота

```bash
npm start
```

### Команды для локальной разработки

```bash
# Создать новую миграцию
npm run migrate

# Применить существующие миграции
npm run migrate:deploy

# Сгенерировать Prisma Client
npm run prisma:generate

# Открыть Prisma Studio (визуальный редактор БД)
npm run prisma:studio
# Откроется на http://localhost:5555
```

## Проверка работы

### 1. Проверка контейнеров

```bash
docker compose ps
```

Должны быть запущены два контейнера:
- `max_hackaton-db-1` (PostgreSQL)
- `max_hackaton-bot-1` (Бот)

### 2. Проверка логов

```bash
docker compose logs bot
```

В логах не должно быть ошибок. Должны быть сообщения о:
- Подключении к базе данных
- Применении миграций
- Запуске бота

### 3. Проверка базы данных

#### Через Prisma Studio

```bash
# В Docker
docker compose exec bot npx prisma studio

# Или локально
npm run prisma:studio
```

Откройте http://localhost:5555 в браузере.

#### Через psql

```bash
# В Docker
docker compose exec db psql -U postgres -d productivity_bot

# Локально
psql -U postgres -d productivity_bot
```

### 4. Тестирование бота

1. Откройте MAX мессенджер
2. Найдите вашего бота
3. Отправьте команду `/start`
4. Проверьте, что бот отвечает и показывает главное меню

## Основные команды бота

- `/start` - Начать работу с ботом
- `/menu` - Главное меню
- `/cancel` - Отменить текущий диалог
- `/stop` - Остановить Pomodoro таймер

### Функции бота

- **Задачи**: создание, просмотр, завершение, удаление задач с дедлайнами
- **События**: создание и управление событиями с напоминаниями
- **Pomodoro**: таймер продуктивности с настраиваемыми циклами и статистикой
- **Настройки**: настройка времени дайджеста, часового пояса, параметров Pomodoro

## Структура проекта

```
max_hackaton/
├── src/                    # Исходный код
│   ├── bot/               # Код бота
│   │   ├── bot.js        # Основной файл бота
│   │   └── dbStore.js    # Адаптер для работы с БД
│   ├── services/         # Сервисы
│   │   ├── notifications.js # Сервис уведомлений
│   │   └── pomodoro.js   # Менеджер Pomodoro
│   ├── database/         # Работа с базой данных
│   │   ├── prisma.js     # Prisma клиент
│   │   └── repositories/ # Репозитории для работы с БД
│   ├── config/           # Конфигурация
│   │   └── env.js        # Загрузка и валидация переменных окружения
│   └── utils/            # Утилиты
│       └── encryption.js
├── prisma/                # Prisma схема
│   └── schema.prisma
├── scripts/               # Вспомогательные скрипты
│   └── init-db.sh        # Инициализация БД в Docker
├── docker-compose.yml    # Docker Compose конфигурация
├── Dockerfile            # Docker образ
├── package.json          # Зависимости проекта (npm)
├── requirements.txt      # Список требований и зависимостей
├── .env.example          # Шаблон переменных окружения
└── README.md             # Этот файл
```

## Решение проблем

### Проблема: Бот не запускается

**Решение:**
1. Проверьте, что файл `.env` создан и содержит `BOT_TOKEN`
2. Проверьте логи: `docker compose logs bot`
3. Убедитесь, что база данных запущена: `docker compose ps`

### Проблема: Ошибка подключения к базе данных

**Решение:**
1. Проверьте, что контейнер базы данных запущен: `docker compose ps`
2. Проверьте логи базы данных: `docker compose logs db`
3. Убедитесь, что в `.env` указаны правильные параметры БД

### Проблема: Миграции не применяются

**Решение:**
1. Остановите контейнеры: `docker compose down`
2. Удалите том базы данных: `docker compose down -v`
3. Запустите заново: `docker compose up --build`

### Проблема: Бот не отвечает в MAX мессенджере

**Решение:**
1. Проверьте, что токен бота правильный в файле `.env`
2. Проверьте логи бота на наличие ошибок
3. Убедитесь, что бот запущен: `docker compose ps`

## Технологии

- **Node.js** - Runtime
- **PostgreSQL** - База данных
- **Prisma** - ORM
- **Docker** - Контейнеризация
- **@maxhub/max-bot-api** - API для MAX мессенджера
- **date-fns** - Работа с датами
- **node-schedule** - Планирование задач

## Лицензия

MIT

## Поддержка

Если возникли проблемы:

1. Проверьте логи: `docker compose logs bot`
2. Убедитесь, что `.env` настроен правильно
3. Проверьте, что БД запущена: `docker compose ps`
4. Проверьте статус контейнеров: `docker compose ps`

---

**Удачи! 🚀**
