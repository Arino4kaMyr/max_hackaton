# 🤖 MAX Productivity Bot

Чат-бот для MAX мессенджера с функциями управления задачами, событиями, Pomodoro таймером и уведомлениями.

## ✨ Возможности

- ✅ **Управление задачами** - создание, просмотр, удаление задач с дедлайнами
- 📅 **События** - планирование событий с напоминаниями
- 🍅 **Pomodoro таймер** - таймер продуктивности с настраиваемыми циклами
- 🔔 **Уведомления** - ежедневные дайджесты и напоминания о событиях
- 💾 **PostgreSQL** - надежное хранение данных через Prisma ORM
- 🐳 **Docker** - легкий запуск через Docker Compose

## 🚀 Быстрый старт

### Вариант 1: Docker Compose (рекомендуется)

```bash
# 1. Создайте .env файл
cp .env.example .env
# Отредактируйте .env и добавьте BOT_TOKEN

# 2. Запустите проект
docker compose up --build
```

### Вариант 2: Локальный запуск

```bash
# 1. Установите зависимости
npm install
cd bot && npm install && cd ..

# 2. Настройте .env
cp .env.example .env
# Отредактируйте .env

# 3. Примените миграции
npm run prisma:generate
npm run migrate

# 4. Запустите бота
cd bot && npm start
```

## 📁 Структура проекта

```
max_hackaton/
├── bot/                    # Код бота
│   ├── src/
│   │   ├── bot.js         # Основной файл бота
│   │   ├── dbStore.js     # Адаптер для работы с БД
│   │   ├── notifications.js # Сервис уведомлений
│   │   └── pomodoro.js    # Менеджер Pomodoro
│   └── package.json
├── src/                    # Общий код
│   └── database/
│       ├── prisma.js      # Prisma клиент
│       └── repositories/  # Репозитории для работы с БД
├── prisma/                 # Prisma схема
│   └── schema.prisma
├── docs/                   # Документация
│   ├── QUICKSTART.md      # Подробная инструкция по запуску
│   ├── INTEGRATION.md     # Интеграция с БД
│   ├── CHECK_DATABASE.md  # Как проверить данные в БД
│   └── SETUP_MIGRATIONS.md
├── scripts/                # Вспомогательные скрипты
│   ├── init-db.sh         # Инициализация БД в Docker
│   ├── start.sh           # Скрипт запуска
│   └── open-prisma-studio.sh
├── docker-compose.yml     # Docker Compose конфигурация
├── Dockerfile             # Docker образ
└── README.md              # Этот файл
```

## 📚 Документация

Вся подробная документация находится в папке [`docs/`](./docs/):

- **[QUICKSTART.md](./docs/QUICKSTART.md)** - Подробная инструкция по запуску и настройке
- **[INTEGRATION.md](./docs/INTEGRATION.md)** - Интеграция бота с базой данных
- **[CHECK_DATABASE.md](./docs/CHECK_DATABASE.md)** - Как проверить данные в БД
- **[SETUP_MIGRATIONS.md](./docs/SETUP_MIGRATIONS.md)** - Работа с миграциями

## 🛠 Команды

### Основные команды

```bash
# Запуск через Docker
docker compose up

# Пересборка и запуск
docker compose up --build

# Остановка
docker compose down

# Логи
docker compose logs -f bot
```

### Prisma команды

```bash
# Создать миграцию
npm run migrate

# Применить миграции (в продакшене)
npm run migrate:deploy

# Сгенерировать Prisma Client
npm run prisma:generate

# Открыть Prisma Studio
npm run prisma:studio
# или
./scripts/open-prisma-studio.sh
```

## ⚙️ Настройка

Создайте файл `.env` в корне проекта:

```env
# PostgreSQL (для Docker Compose)
DB_USER=postgres
DB_PASSWORD=postgres
DB_NAME=productivity_bot
DB_PORT=5432

# Для локального запуска
# DATABASE_URL="postgresql://user:password@localhost:5432/productivity_bot?schema=public"

# Бот
BOT_TOKEN=ваш_токен_бота
TIMEZONE=Europe/Moscow
PORT=3000
```

## 🧪 Проверка работы

### Проверить данные в БД

```bash
# Prisma Studio (визуальный редактор)
./scripts/open-prisma-studio.sh
# Откройте http://localhost:5555

# Или через psql
docker compose exec db psql -U postgres -d productivity_bot
```

### Тестирование бота

1. Откройте MAX мессенджер
2. Найдите вашего бота
3. Отправьте `/start`
4. Создайте задачу, событие, запустите Pomodoro

## 📝 Основные команды бота

- `/start` - Начать работу с ботом
- `/menu` - Главное меню
- `/cancel` - Отменить текущий диалог
- `/stop` - Остановить Pomodoro таймер

## 🔧 Технологии

- **Node.js** - Runtime
- **PostgreSQL** - База данных
- **Prisma** - ORM
- **Docker** - Контейнеризация
- **@maxhub/max-bot-api** - API для MAX мессенджера
- **date-fns** - Работа с датами
- **node-schedule** - Планирование задач

## 📄 Лицензия

MIT

## 🤝 Поддержка

Если возникли проблемы:

1. Проверьте логи: `docker compose logs bot`
2. Проверьте документацию в папке `docs/`
3. Убедитесь, что `.env` настроен правильно
4. Проверьте, что БД запущена: `docker compose ps`

---

**Удачи на хакатоне! 🚀**
