# 📁 Структура проекта

## Общая структура

```
max_hackaton/
├── bot/                          # Код бота
│   ├── src/
│   │   ├── bot.js               # Основной файл бота (обработчики команд)
│   │   ├── dbStore.js          # Адаптер для работы с PostgreSQL
│   │   ├── notifications.js    # Сервис уведомлений
│   │   └── pomodoro.js         # Менеджер Pomodoro таймера
│   └── package.json
│
├── src/                          # Общий код проекта
│   └── database/
│       ├── prisma.js           # Prisma Client (singleton)
│       └── repositories/        # Репозитории для работы с БД
│           ├── userRepository.js
│           ├── taskRepository.js
│           ├── eventRepository.js
│           ├── userSettingsRepository.js
│           └── pomodoroRepository.js
│
├── prisma/                       # Prisma схема и миграции
│   └── schema.prisma           # Схема базы данных
│
├── docs/                         # Документация
│   ├── README.md               # Навигация по документации
│   ├── QUICKSTART.md           # Инструкция по запуску
│   ├── INTEGRATION.md          # Интеграция с БД
│   ├── CHECK_DATABASE.md      # Проверка данных в БД
│   ├── SETUP_MIGRATIONS.md    # Работа с миграциями
│   └── PROJECT_STRUCTURE.md    # Этот файл
│
├── scripts/                      # Вспомогательные скрипты
│   ├── init-db.sh             # Инициализация БД в Docker
│   ├── start.sh                # Скрипт запуска проекта
│   └── open-prisma-studio.sh  # Открыть Prisma Studio
│
├── .env.example                 # Пример файла с переменными окружения
├── .gitignore                   # Игнорируемые файлы для Git
├── docker-compose.yml          # Docker Compose конфигурация
├── Dockerfile                  # Docker образ
├── package.json                # Зависимости и скрипты (корень)
└── README.md                   # Главный README проекта
```

## Описание директорий

### `bot/`
Основной код бота для MAX мессенджера.

- **`bot.js`** - Главный файл, содержит:
  - Инициализацию бота
  - Обработчики команд (`/start`, `/menu`, `/stop`)
  - Обработчики callback-кнопок
  - Диалоговые флоу для создания задач/событий
  - Интеграцию с хранилищем и сервисами

- **`dbStore.js`** - Адаптер для работы с PostgreSQL через Prisma:
  - Преобразует UUID из БД в числовые ID для бота
  - Сохраняет совместимость с API бота
  - Использует репозитории из `src/database/repositories/`

- **`notifications.js`** - Сервис уведомлений:
  - Ежедневные дайджесты
  - Напоминания о событиях
  - Использует `node-schedule` для планирования

- **`pomodoro.js`** - Менеджер Pomodoro таймера:
  - Управление сессиями таймеров
  - Циклы работы/отдыха
  - Уведомления о смене фаз


### `src/database/`
Общий код для работы с базой данных.

- **`prisma.js`** - Singleton для Prisma Client:
  - Инициализация подключения
  - Управление жизненным циклом
  - Логирование запросов в development

- **`repositories/`** - Репозитории для работы с таблицами:
  - `userRepository.js` - Пользователи
  - `taskRepository.js` - Задачи
  - `eventRepository.js` - События
  - `userSettingsRepository.js` - Настройки пользователей
  - `pomodoroRepository.js` - Pomodoro сессии

### `prisma/`
Prisma схема и миграции.

- **`schema.prisma`** - Определение схемы БД:
  - Модели (User, Task, Event, UserSettings, PomodoroSession)
  - Связи между таблицами
  - Индексы

### `docs/`
Вся документация проекта.

- **`README.md`** - Навигация по документации
- **`QUICKSTART.md`** - Подробная инструкция по запуску
- **`INTEGRATION.md`** - Интеграция бота с БД
- **`CHECK_DATABASE.md`** - Как проверить данные в БД
- **`SETUP_MIGRATIONS.md`** - Работа с миграциями
- **`PROJECT_STRUCTURE.md`** - Этот файл

### `scripts/`
Вспомогательные скрипты для разработки и запуска.

- **`init-db.sh`** - Инициализация БД в Docker:
  - Применение миграций или `db push`
  - Запуск бота после инициализации

- **`start.sh`** - Универсальный скрипт запуска:
  - Определяет способ запуска (Docker/локально)
  - Применяет миграции
  - Запускает проект

- **`open-prisma-studio.sh`** - Открыть Prisma Studio:
  - Запускает Prisma Studio в Docker контейнере
  - Открывает в браузере

## Файлы конфигурации

### `.env.example`
Пример файла с переменными окружения. Скопируйте в `.env` и заполните.

### `docker-compose.yml`
Конфигурация Docker Compose:
- Сервис `db` - PostgreSQL
- Сервис `bot` - Приложение бота

### `Dockerfile`
Docker образ для бота:
- Установка зависимостей
- Генерация Prisma Client
- Инициализация БД
- Запуск бота

### `package.json` (корень)
Зависимости и скрипты для:
- Prisma
- Общих зависимостей

### `bot/package.json`
Зависимости и скрипты для бота:
- @maxhub/max-bot-api
- date-fns
- node-schedule

## Потоки данных

```
MAX Messenger
    ↓
bot/src/bot.js (обработчики)
    ↓
bot/src/dbStore.js (адаптер)
    ↓
src/database/repositories/ (репозитории)
    ↓
src/database/prisma.js (Prisma Client)
    ↓
PostgreSQL
```

## Добавление новых функций

1. **Новая команда бота**: Добавьте обработчик в `bot/src/bot.js`
2. **Новая таблица БД**: Добавьте модель в `prisma/schema.prisma`, создайте репозиторий
3. **Новый сервис**: Создайте файл в `bot/src/` или `src/`
4. **Новая документация**: Добавьте файл в `docs/`

## Важные замечания

- **`store.js`** не используется в продакшене, только для тестирования
- Все данные сохраняются в PostgreSQL через `dbStore.js`
- Сессии диалогов хранятся в памяти (временные)
- Prisma миграции находятся в `prisma/migrations/` (создаются при `npm run migrate`)

