# Создание миграций

Если вы хотите использовать миграции вместо `db push`, выполните:

```bash
# В корне проекта
npm run migrate
```

Это создаст папку `prisma/migrations` с первой миграцией.

Для Docker это не обязательно - `db push` сработает автоматически.

