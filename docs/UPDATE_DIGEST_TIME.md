# 🔄 Применение изменений для настройки времени дайджеста

## Что было добавлено

1. ✅ Поле `dailyDigestTime` в таблице `user_settings`
2. ✅ Кнопка "⏰ Изменить время дайджеста" в настройках
3. ✅ Валидация формата времени
4. ✅ Автоматическое перепланирование задачи при изменении времени

## Что нужно сделать

### 1. Создать миграцию БД

```bash
# В корне проекта
npm run migrate
```

Или если используете Docker:

```bash
# Остановите контейнеры
docker compose down

# Пересоберите и запустите (миграция применится автоматически)
docker compose up --build
```

### 2. Проверить работу

1. Откройте бота в MAX мессенджере
2. Откройте настройки: `/menu` → ⚙️ Настройки
3. Должна появиться кнопка "⏰ Изменить время дайджеста"
4. Нажмите на неё и введите время (например, `08:00`)
5. Проверьте, что время сохранилось в настройках

### 3. Проверить в БД

```bash
# Через Prisma Studio
npm run prisma:studio

# Или через psql
docker compose exec db psql -U postgres -d productivity_bot -c "SELECT daily_digest_time FROM user_settings;"
```

## Формат времени

- ✅ Правильно: `09:00`, `18:30`, `00:00`, `23:59`
- ❌ Неправильно: `9:00`, `25:00`, `09:60`

## По умолчанию

Если пользователь не настроил время, используется `09:00`.

