# 📅 Настройка Яндекс Календаря

## Что реализовано

✅ Интеграция с Яндекс Календарем через CalDAV  
✅ Безопасное хранение паролей приложения (шифрование)  
✅ Автоматическая синхронизация событий  
✅ Подключение через бота  

---

## Как подключить

### Шаг 1: Создайте пароль приложения в Яндекс ID

1. Зайдите на [Яндекс ID](https://id.yandex.ru/)
2. Перейдите в раздел **"Безопасность"**
3. Найдите **"Пароли приложений"**
4. Нажмите **"Создать пароль приложения"**
5. Выберите **"Календарь CalDAV"**
6. Скопируйте созданный пароль (он показывается только один раз!)

### Шаг 2: Подключите в боте

1. Откройте бота
2. Перейдите в **⚙️ Настройки**
3. Нажмите **📅 Яндекс Календарь**
4. Введите ваш логин Яндекс (email)
5. Введите пароль приложения
6. Готово! ✅

---

## Как это работает

### Автоматическая синхронизация

После подключения все новые события, созданные в боте, **автоматически** добавляются в ваш Яндекс Календарь.

### Что синхронизируется:

- ✅ Название события
- ✅ Описание
- ✅ Дата и время
- ✅ Напоминания

### Что НЕ синхронизируется:

- ❌ Задачи (только события)
- ❌ Изменения событий (только создание)
- ❌ Удаление событий

---

## Управление подключением

### Просмотр статуса

**⚙️ Настройки** → **📅 Яндекс Календарь**

Показывает:
- Логин
- Последнюю синхронизацию
- Статус синхронизации

### Отключение

**⚙️ Настройки** → **📅 Яндекс Календарь** → **❌ Отключить**

---

## Безопасность

### Шифрование

Все пароли приложения **шифруются** перед сохранением в базу данных с использованием AES-256-GCM.

### Переменная окружения

Для шифрования нужен ключ `ENCRYPTION_SECRET_KEY` в `.env`:

```bash
# Сгенерируйте ключ командой:
openssl rand -hex 32
```

Добавьте в `.env`:
```env
ENCRYPTION_SECRET_KEY=ваш_64_символьный_hex_ключ
```

---

## Установка зависимостей

После добавления интеграции нужно установить новые зависимости:

```bash
cd bot
npm install
```

Или через Docker:
```bash
docker compose up --build
```

---

## Миграция базы данных

После обновления схемы Prisma нужно применить миграцию:

```bash
# Локально
npm run migrate

# Или через Docker
docker compose exec bot npx prisma migrate dev --name add_yandex_calendar
```

---

## Технические детали

### Схема БД

Добавлена таблица `yandex_calendar_tokens`:
- `user_id` - связь с пользователем
- `yandex_login` - логин (зашифрован)
- `app_password` - пароль приложения (зашифрован)
- `calendar_url` - URL календаря
- `sync_enabled` - включена ли синхронизация
- `last_sync_at` - последняя синхронизация

### CalDAV протокол

Используется стандартный протокол CalDAV для работы с календарем:
- `PUT` - создание/обновление события
- `DELETE` - удаление события
- `REPORT` - получение событий
- `PROPFIND` - проверка доступа

### iCal формат

События создаются в формате iCalendar (RFC 5545) и отправляются в Яндекс Календарь.

---

## Решение проблем

### Ошибка подключения

**Проблема:** "Не удалось подключиться к Яндекс Календарю"

**Решение:**
1. Проверьте правильность логина и пароля приложения
2. Убедитесь, что пароль создан для "Календарь CalDAV"
3. Проверьте, что у вас есть доступ к Яндекс Календарю

### События не синхронизируются

**Проблема:** События создаются в боте, но не появляются в календаре

**Решение:**
1. Проверьте статус подключения в настройках
2. Убедитесь, что синхронизация включена
3. Проверьте логи бота на наличие ошибок

### Ошибка шифрования

**Проблема:** "ENCRYPTION_SECRET_KEY is not set"

**Решение:**
1. Создайте ключ: `openssl rand -hex 32`
2. Добавьте в `.env`: `ENCRYPTION_SECRET_KEY=ваш_ключ`
3. Перезапустите бота

---

## Ограничения

1. **Только создание событий** - изменения и удаления не синхронизируются
2. **Односторонняя синхронизация** - события из календаря не импортируются в бота
3. **Только события** - задачи не синхронизируются
4. **Длительность события** - по умолчанию 1 час

---

## Будущие улучшения

- [ ] Двусторонняя синхронизация
- [ ] Синхронизация изменений событий
- [ ] Синхронизация удаления событий
- [ ] Импорт событий из календаря
- [ ] Настройка длительности события

