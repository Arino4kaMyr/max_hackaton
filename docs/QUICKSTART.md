# 🚀 Быстрый старт

## Вариант 1: Запуск через Docker Compose (рекомендуется)

Это самый простой способ - все запустится автоматически.

### Шаг 1: Создайте `.env` файл

В корне проекта создайте файл `.env`:

```env
# PostgreSQL (для Docker Compose)
DB_USER=postgres
DB_PASSWORD=postgres
DB_NAME=productivity_bot
DB_PORT=5432

# Бот
BOT_TOKEN=ваш_токен_бота_здесь
TIMEZONE=Europe/Moscow

# DATABASE_URL будет сформирован автоматически из переменных выше
# Или можете указать явно:
# DATABASE_URL=postgresql://postgres:postgres@db:5432/productivity_bot?schema=public
```

### Шаг 2: Запустите через Docker Compose

```bash
# Запустить БД и бота
docker compose up --build

# Или в фоновом режиме
docker compose up -d --build
```

**Что произойдет:**
- ✅ Запустится PostgreSQL в контейнере
- ✅ Автоматически применятся миграции Prisma
- ✅ Запустится бот

### Шаг 3: Проверьте логи

```bash
# Посмотреть логи
docker compose logs -f bot

# Посмотреть логи БД
docker compose logs -f db
```

### Остановка

```bash
# Остановить все
docker compose down

# Остановить и удалить данные БД
docker compose down -v
```

---

## Вариант 2: Локальный запуск (без Docker)

Если у вас уже установлен PostgreSQL локально.

### Шаг 1: Установите PostgreSQL

**macOS:**
```bash
brew install postgresql@15
brew services start postgresql@15
```

**Linux (Ubuntu/Debian):**
```bash
sudo apt-get update
sudo apt-get install postgresql postgresql-contrib
sudo systemctl start postgresql
```

**Windows:**
Скачайте и установите с [официального сайта](https://www.postgresql.org/download/windows/)

### Шаг 2: Создайте базу данных

```bash
# Подключитесь к PostgreSQL
psql -U postgres

# В psql выполните:
CREATE DATABASE productivity_bot;
CREATE USER bot_user WITH PASSWORD 'your_password';
GRANT ALL PRIVILEGES ON DATABASE productivity_bot TO bot_user;
\q
```

### Шаг 3: Создайте `.env` файл

В корне проекта:

```env
# PostgreSQL
DATABASE_URL="postgresql://bot_user:your_password@localhost:5432/productivity_bot?schema=public"

# Бот
BOT_TOKEN=ваш_токен_бота_здесь
TIMEZONE=Europe/Moscow
```

### Шаг 4: Установите зависимости

```bash
# В корне проекта
npm install

# В папке bot
cd bot
npm install
cd ..
```

### Шаг 5: Примените миграции

```bash
# Сгенерируйте Prisma Client
npm run prisma:generate

# Создайте и примените миграции
npm run migrate
```

### Шаг 6: Запустите бота

```bash
cd bot
npm start
```

---

## 🧪 Проверка работы

### 1. Проверьте подключение к БД

```bash
# Откройте Prisma Studio (визуальный редактор БД)
npm run prisma:studio
```

Откроется браузер на `http://localhost:5555` - там вы увидите все таблицы и данные.

### 2. Проверьте бота

1. Откройте MAX мессенджер
2. Найдите вашего бота
3. Отправьте команду `/start`
4. Попробуйте создать задачу через меню

### 3. Проверьте логи

Если что-то не работает, проверьте логи:

**Docker:**
```bash
docker compose logs bot
```

**Локально:**
Логи будут в консоли, где запущен бот.

---

## 🔍 Проверка данных в БД

### Через Prisma Studio

```bash
npm run prisma:studio
```

### Через psql

```bash
# Подключитесь к БД
psql -U postgres -d productivity_bot

# Посмотрите таблицы
\dt

# Посмотрите пользователей
SELECT * FROM users;

# Посмотрите задачи
SELECT * FROM tasks;

# Выйти
\q
```

### Через Docker

```bash
# Подключиться к БД в контейнере
docker compose exec db psql -U postgres -d productivity_bot
```

---

## 🐛 Решение проблем

### Ошибка: "Can't reach database server"

**Решение:**
- Проверьте, что PostgreSQL запущен
- Проверьте `DATABASE_URL` в `.env`
- Проверьте, что порт 5432 не занят

### Ошибка: "Migration failed"

**Решение:**
```bash
# Сбросьте миграции (ОСТОРОЖНО: удалит все данные!)
npm run migrate reset

# Или создайте новую миграцию
npm run migrate
```

### Ошибка: "Prisma Client not generated"

**Решение:**
```bash
npm run prisma:generate
```

### Бот не отвечает

**Решение:**
- Проверьте `BOT_TOKEN` в `.env`
- Проверьте логи: `docker compose logs bot` или в консоли
- Убедитесь, что бот запущен

---

## 📝 Полезные команды

```bash
# Создать новую миграцию
npm run migrate

# Применить миграции (в продакшене)
npm run migrate:deploy

# Сгенерировать Prisma Client
npm run prisma:generate

# Открыть Prisma Studio
npm run prisma:studio

# Остановить Docker контейнеры
docker compose down

# Перезапустить контейнеры
docker compose restart

# Посмотреть статус контейнеров
docker compose ps
```

---

## 🎯 Следующие шаги

1. ✅ Запустите БД и бота
2. ✅ Протестируйте создание задач
3. ✅ Протестируйте создание событий
4. ✅ Протестируйте Pomodoro таймер
5. ✅ Проверьте уведомления

Удачи! 🚀

