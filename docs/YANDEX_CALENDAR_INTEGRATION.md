# 📅 Интеграция с Яндекс Календарем: Оценка сложности

## 🎯 Оценка сложности: **Средняя** (4-6 часов работы)

### Почему это проще, чем Google Calendar:
1. **CalDAV протокол** - стандартизированный протокол, есть готовые библиотеки
2. **Пароль приложения** - проще, чем OAuth 2.0 (но менее безопасно)
3. **Меньше шагов авторизации** - не нужен полный OAuth flow
4. **Прямое подключение** - можно использовать логин/пароль приложения

### Но есть нюансы:
- CalDAV требует понимания протокола
- Меньше документации и примеров, чем у Google
- Пароль приложения менее безопасен, чем OAuth токены

---

## 📋 Что нужно сделать

### 1. Создание пароля приложения (10 минут)

#### Шаги:
1. Зайти в [Яндекс ID](https://id.yandex.ru/)
2. Перейти в "Безопасность"
3. Создать "Пароль приложения"
4. Выбрать "Календарь CalDAV"
5. Сохранить пароль (он показывается только один раз!)

#### Сложность: ⭐ (Очень простая)

---

### 2. Обновление схемы БД (30 минут)

#### Нужно добавить таблицу для хранения данных подключения:

```prisma
model YandexCalendarToken {
  id            String   @id @default(uuid())
  userId        String   @unique @map("user_id")
  yandexLogin   String   @map("yandex_login")        // Зашифрован!
  appPassword   String   @map("app_password")        // Зашифрован!
  calendarUrl   String?  @map("calendar_url")         // URL календаря
  syncEnabled   Boolean  @default(true) @map("sync_enabled")
  lastSyncAt    DateTime? @map("last_sync_at")
  createdAt     DateTime @default(now()) @map("created_at")
  updatedAt     DateTime @updatedAt @map("updated_at")

  user User @relation(fields: [userId], references: [id], onDelete: Cascade)

  @@map("yandex_calendar_tokens")
}
```

#### Обновить модель User:
```prisma
model User {
  // ... существующие поля
  yandexCalendarToken YandexCalendarToken?
}
```

#### Сложность: ⭐ (Очень простая)

---

### 3. Безопасное хранение паролей (1 час)

#### ⚠️ КРИТИЧЕСКИЙ МОМЕНТ: Пароли нельзя хранить в открытом виде!

#### Шифрование (аналогично Google):

```javascript
import crypto from 'crypto';

const ALGORITHM = 'aes-256-gcm';
const SECRET_KEY = process.env.ENCRYPTION_SECRET_KEY; // 32 байта

function encrypt(text) {
  const iv = crypto.randomBytes(16);
  const cipher = crypto.createCipheriv(ALGORITHM, Buffer.from(SECRET_KEY, 'hex'), iv);
  
  let encrypted = cipher.update(text, 'utf8', 'hex');
  encrypted += cipher.final('hex');
  
  const authTag = cipher.getAuthTag();
  
  return {
    encrypted,
    iv: iv.toString('hex'),
    authTag: authTag.toString('hex')
  };
}

function decrypt(encryptedData) {
  const decipher = crypto.createDecipheriv(
    ALGORITHM,
    Buffer.from(SECRET_KEY, 'hex'),
    Buffer.from(encryptedData.iv, 'hex')
  );
  
  decipher.setAuthTag(Buffer.from(encryptedData.authTag, 'hex'));
  
  let decrypted = decipher.update(encryptedData.encrypted, 'hex', 'utf8');
  decrypted += decipher.final('utf8');
  
  return decrypted;
}
```

#### Сложность: ⭐⭐⭐ (Средняя, требует понимания криптографии)

---

### 4. Работа с CalDAV (2-3 часа)

#### CalDAV - это протокол на основе WebDAV для календарей

#### Необходимые зависимости:

```bash
npm install ical.js      # Для парсинга iCal формата
npm install node-caldav  # Библиотека для работы с CalDAV
# или
npm install caldav       # Альтернативная библиотека
```

#### Пример подключения:

```javascript
import { createClient } from 'node-caldav';

async function getYandexCalendarClient(userId) {
  const tokenData = await getYandexToken(userId);
  
  const client = createClient({
    url: 'https://caldav.yandex.ru',
    username: decrypt(tokenData.yandexLogin),
    password: decrypt(tokenData.appPassword),
  });
  
  return client;
}
```

#### Сложность: ⭐⭐⭐ (Средняя, нужно изучить CalDAV)

---

### 5. Создание события в Яндекс Календаре (1 час)

#### Используя CalDAV:

```javascript
import { createClient } from 'node-caldav';
import { createEvent } from 'ical.js';

async function syncEventToYandex(userId, event) {
  const client = await getYandexCalendarClient(userId);
  
  // Формируем iCal событие
  const icalEvent = createEvent({
    start: event.datetime,
    end: new Date(event.datetime.getTime() + 60 * 60 * 1000), // +1 час
    summary: event.title,
    description: event.description || '',
    location: '',
    url: '',
    categories: [],
    status: 'CONFIRMED',
    busyStatus: 'BUSY',
    organizer: { email: decrypt(tokenData.yandexLogin) },
    attendees: [],
    alarms: event.reminderMinutes ? [{
      action: 'DISPLAY',
      trigger: { minutes: event.reminderMinutes },
    }] : [],
  });
  
  // Конвертируем в строку iCal
  const icalString = icalEvent.toString();
  
  // Создаем событие через CalDAV
  const calendarUrl = tokenData.calendarUrl || 'https://caldav.yandex.ru/calendars/USERNAME/events/';
  const eventUrl = `${calendarUrl}${event.id}.ics`;
  
  await client.putEvent(eventUrl, icalString);
  
  // Сохраняем URL события для связи
  await updateEvent(event.id, { yandexEventUrl: eventUrl });
}
```

#### Сложность: ⭐⭐⭐ (Средняя, нужно работать с iCal форматом)

---

### 6. Получение событий из Яндекс Календаря (1-2 часа)

#### Чтение через CalDAV:

```javascript
async function syncFromYandex(userId) {
  const client = await getYandexCalendarClient(userId);
  const lastSync = await getLastSyncTime(userId);
  
  const calendarUrl = tokenData.calendarUrl || 'https://caldav.yandex.ru/calendars/USERNAME/events/';
  
  // Получаем список событий
  const events = await client.getEvents(calendarUrl, {
    start: lastSync,
    end: new Date(),
  });
  
  // Парсим iCal события
  for (const icalEvent of events) {
    const eventData = parseICalEvent(icalEvent);
    
    // Проверяем, существует ли уже событие
    const existing = await findEventByYandexUrl(icalEvent.url);
    
    if (!existing) {
      // Создаем новое событие в боте
      await createEvent(userId, {
        title: eventData.summary,
        description: eventData.description,
        datetime: eventData.start,
        yandexEventUrl: icalEvent.url,
      });
    } else {
      // Обновляем существующее
      await updateEvent(existing.id, {
        title: eventData.summary,
        datetime: eventData.start,
      });
    }
  }
  
  await updateLastSyncTime(userId, new Date());
}

function parseICalEvent(icalString) {
  // Парсим iCal строку с помощью ical.js
  const jcalData = ICAL.parse(icalString);
  const comp = new ICAL.Component(jcalData);
  const vevent = comp.getFirstSubcomponent('vevent');
  const event = new ICAL.Event(vevent);
  
  return {
    summary: event.summary,
    description: event.description,
    start: event.startDate.toJSDate(),
    end: event.endDate.toJSDate(),
    location: event.location,
  };
}
```

#### Сложность: ⭐⭐⭐⭐ (Высокая, нужно парсить iCal формат)

---

## 🔒 Безопасность: Критические моменты

### ✅ Обязательно:

1. **Шифрование паролей в БД**
   - Никогда не храните пароли в открытом виде
   - Используйте AES-256-GCM или аналогичное шифрование
   - Храните ключ шифрования в переменных окружения

2. **HTTPS для CalDAV**
   - Яндекс CalDAV работает через HTTPS
   - Не используйте HTTP

3. **Пароль приложения, а не основной пароль**
   - Всегда используйте пароль приложения
   - Никогда не храните основной пароль пользователя

4. **Ограничение прав**
   - Пароль приложения дает доступ только к календарю
   - Это безопаснее, чем основной пароль

### ⚠️ Риски:

- **Утечка пароля приложения** → Злоумышленник получит доступ к календарю
- **Man-in-the-middle** → Если не использовать HTTPS
- **Пароль приложения можно отозвать** → Нужна обработка ошибок авторизации

---

## 📦 Необходимые зависимости

```json
{
  "dependencies": {
    "node-caldav": "^1.0.0",      // Для работы с CalDAV
    "ical.js": "^1.5.0",           // Для парсинга iCal
    "crypto": "^1.0.1"             // Для шифрования (встроена в Node.js)
  }
}
```

Или альтернативные библиотеки:
```json
{
  "dependencies": {
    "caldav": "^0.1.0",
    "ical.js": "^1.5.0"
  }
}
```

---

## 🎯 Рекомендуемый подход

### Этап 1: Минимальная интеграция (MVP)
1. Только односторонняя синхронизация (бот → Яндекс)
2. Простое шифрование паролей
3. Базовое создание событий через CalDAV

**Время: 2-3 часа**

### Этап 2: Полная интеграция
1. Двусторонняя синхронизация
2. Парсинг iCal событий
3. Обработка конфликтов
4. Автоматическая синхронизация по расписанию

**Время: +2-3 часа**

---

## 🔄 Сравнение с Google Calendar

| Параметр | Яндекс Календарь | Google Calendar |
|----------|------------------|-----------------|
| **Протокол** | CalDAV | REST API |
| **Авторизация** | Пароль приложения | OAuth 2.0 |
| **Сложность** | Средняя | Средняя-Высокая |
| **Время** | 4-6 часов | 6-8 часов |
| **Безопасность** | Средняя | Высокая |
| **Документация** | Меньше | Больше |
| **Библиотеки** | Меньше | Больше |

### Преимущества Яндекс:
- ✅ Проще авторизация (пароль приложения)
- ✅ Стандартизированный протокол (CalDAV)
- ✅ Меньше шагов для подключения

### Недостатки Яндекс:
- ❌ Меньше документации
- ❌ Меньше готовых библиотек
- ❌ Пароль приложения менее безопасен, чем OAuth
- ❌ Нужно работать с iCal форматом

---

## 💡 Альтернативные подходы

### 1. Использовать готовые сервисы
- **Zapier** / **Make.com** - готовые интеграции
- **n8n** - open-source, можно развернуть самому

### 2. Упрощенный подход
- Только экспорт событий (без синхронизации обратно)
- Пользователь вручную подключает календарь через iCal ссылку

### 3. Использовать Яндекс API (если доступен)
- Проверить, есть ли REST API у Яндекс Календаря
- Может быть проще, чем CalDAV

---

## 📊 Итоговая оценка

| Компонент | Сложность | Время |
|-----------|-----------|-------|
| Создание пароля приложения | ⭐ | 10 мин |
| Схема БД | ⭐ | 30 мин |
| Шифрование паролей | ⭐⭐⭐ | 1 час |
| CalDAV подключение | ⭐⭐⭐ | 1 час |
| Создание событий | ⭐⭐⭐ | 1 час |
| Получение событий | ⭐⭐⭐⭐ | 1-2 часа |
| Обработка ошибок | ⭐⭐⭐ | 1 час |
| **ИТОГО** | **⭐⭐⭐** | **4-6 часов** |

---

## 🚀 Быстрый старт (если решите делать)

1. Создайте пароль приложения в Яндекс ID
2. Добавьте таблицу для токенов в Prisma
3. Установите библиотеки для CalDAV
4. Реализуйте шифрование паролей
5. Реализуйте создание событий через CalDAV
6. Добавьте синхронизацию (опционально)

**Рекомендация:** Начните с MVP (односторонняя синхронизация), затем расширяйте функционал.

---

## 📚 Полезные ресурсы

- [CalDAV протокол (RFC 4791)](https://tools.ietf.org/html/rfc4791)
- [iCal формат (RFC 5545)](https://tools.ietf.org/html/rfc5545)
- [Яндекс ID - Пароли приложений](https://id.yandex.ru/)
- [node-caldav библиотека](https://www.npmjs.com/package/node-caldav)
- [ical.js библиотека](https://www.npmjs.com/package/ical.js)

---

## ❓ Вопросы для обсуждения

1. Нужна ли двусторонняя синхронизация или достаточно экспорта в Яндекс?
2. Как часто синхронизировать? (реал-тайм, раз в час, по запросу)
3. Что делать при конфликтах? (приоритет боту или Яндекс?)
4. Нужна ли синхронизация задач или только событий?

---

## ⚠️ Важные замечания

1. **CalDAV URL может отличаться** - проверьте актуальный URL для вашего региона
2. **iCal формат** - нужно правильно парсить и генерировать события
3. **Часовые пояса** - важно правильно обрабатывать временные зоны
4. **Ошибки авторизации** - пароль приложения может быть отозван, нужна обработка

---

## 🎯 Вывод

Интеграция с Яндекс Календарем **проще**, чем с Google Calendar, но требует работы с CalDAV протоколом и iCal форматом. Основное преимущество - более простая авторизация через пароль приложения.

**Рекомендация:** Если нужна быстрая интеграция и вы готовы работать с CalDAV - Яндекс хороший выбор. Если нужна более безопасная и документированная интеграция - лучше Google Calendar.

